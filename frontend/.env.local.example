# Copy to .env.local and adjust if your API runs on a different host/port.
NEXT_PUBLIC_API_URL=http://localhost:5001/api

# Optional: only needed if your Socket.IO server runs on a different host
# than the REST API above. Defaults to NEXT_PUBLIC_API_URL with "/api" stripped.
NEXT_PUBLIC_SOCKET_URL=http://localhost:5001
